#pragma once;

#include <string>
#include <sstream>

using namespace std;

namespace Common{

	enum user{
		DIEGO,
		GENERAL
	};

	enum instanceSymmetry{
		ASYMMETRIC,
		SYMMETRIC
	};

namespace Common{

	enum user{
		Ismahane,
		GENERAL
	};

	enum instanceSymmetry{
		ASYMMETRIC,
		SYMMETRIC
	};
}
}
